alert: ApiGatewayRequestRateIncrease
expr: (rate(http_requests_total{service="api-gateway"}[5m]) - rate(http_requests_total{service="api-gateway"}[1h]))
  / rate(http_requests_total{service="api-gateway"}[1h]) > 0.5
labels:
  severity: warning
  team: Retroshift
  component: api
annotations:
  summary: Request rate increase
  description: Alert when request rate increases more than 50% compared to previous
    hour
